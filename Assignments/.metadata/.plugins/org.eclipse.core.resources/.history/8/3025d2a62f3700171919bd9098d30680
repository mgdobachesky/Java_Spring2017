package doctorLab;

public class Billing {
	// Define class properties
	private String patientName;
	private String doctorName;
	private double billingAmount;
	
	// Define class constructor
	public Billing(Doctor doctor, Patient patient) {
		patientName = patient.getName();
		doctorName = doctor.getName();
		billingAmount = doctor.getOfficeVisitFee();
	}
	
	// Define class methods
	public void writeOutput() {
		System.out.println( "Doctor: " + doctorName + "\nPatient: " + patientName + "\nBilling Amount: $" + billingAmount + "\n===============================\n");
	}
	
	public boolean equals(Billing billing) {
		if(patientName.equals(billing.patientName) && doctorName.equals(billing.doctorName) && ((billingAmount - billing.billingAmount) < 0.001)) {
			return true;
		} else {
			return false;
		}
	}
}
